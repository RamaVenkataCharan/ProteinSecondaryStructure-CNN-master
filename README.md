# ProteinSecondaryStructure-CNN-master
The project uses CNN architecture to automatically learn sequence patterns and local structural relationships from protein data. Protein sequences are first preprocessed and converted into numerical feature representations that can be used as input for the neural network. 
